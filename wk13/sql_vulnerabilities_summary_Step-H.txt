1 -> No Data was returned, just the column headers appeared. It was expected because no database rows matched. 

2 -> No Data was returned, just the column headers appeared. It was expected because no database rows matched. 

3 -> No Data was returned, just the column headers appeared. It was expected because no database rows matched. 
